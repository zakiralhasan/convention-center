/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["i.ibb.co"],
        formats: ["image/webp"],
    },
}

module.exports = nextConfig
